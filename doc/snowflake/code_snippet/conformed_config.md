```sql
INSERT INTO DEV.CONFORMED.ETL_CONTROL_FLOW (
    USECASE, 
    SOURCE_TABLE_NAME, 
    SOURCE_SCHEMANAME, 
    TARGET_TABLE_NAME, 
    TARGET_SCHEMANAME, 
    LOAD_TYPE, 
    PROCEDURE_NAME)
VALUES (
    '<USE CASE>',
    '<SOURCE VIEW NAME>',
    '<SOURCE VIEW SCHEMA NAME>',
    '<TARGET TABLE NAME>',
    '<TARGET TABLE SCHEMA NAME>',
    '<INCREMENTAL/FULL/SNAPSHOT>',
    '<STORED PROCEDURE NAME>'
    );
```
Example,
```sql
INSERT INTO DEV.CONFORMED.ETL_CONTROL_FLOW (
    USECASE, 
    SOURCE_TABLE_NAME, 
    SOURCE_SCHEMANAME, 
    TARGET_TABLE_NAME, 
    TARGET_SCHEMANAME, 
    LOAD_TYPE, 
    PROCEDURE_NAME)
VALUES (
    'POWERBIGOVERNANCE',
    'V_DIM_PBIGOV_DATASETUPSTREAMDATASETS',
    'CONFORMED',
    'DIMDATASETUPSTREAMDATASETS',
    'PBIGOV',
    'INCREMENTAL',
    'DIMPBIGOV_DATASETUPSTREAMDATASETS_ADD'
    );
```

**Note. SOURCE_TABLE_NAME could be table or view. As per best practice it should be view.**
