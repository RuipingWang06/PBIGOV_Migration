```sql
CREATE OR REPLACE TABLE <TABLE NAME> (
    <TABLE NAME>KEY NUMBER(38,0) NOT NULL AUTOINCREMENT START 1 INCREMENT 1,
    <COLUMN 1 NAME> <EXTRA SETTINGS>,
    <COLUMN 2 NAME> <EXTRA SETTINGS>,
    ...
    <COLUMN N NAME> <EXTRA SETTINGS>,
    DATASOURCEKEY NUMBER(38,0) NOT NULL, 
    DELETEFLAG BOOLEAN NOT NULL DEFAULT FALSE, 
    EXTRACTEDON TIMESTAMP_NTZ(9), 
    CREATEDON DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP(),
    CREATEDBY VARCHAR(100) NOT NULL DEFAULT CURRENT_USER(),
    UPDATEDON DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP(),
    UPDATEDBY VARCHAR(100) NOT NULL DEFAULT CURRENT_USER(),
    primary key (<TABLE NAME>KEY), 
    constraint FDATASOURCEKEY foreign key (DATASOURCEKEY) references PUBLIC.DIMDATASOURCES(DATASOURCEKEY) 
);
```
Example,
```sql
CREATE OR REPLACE TABLE DIMDATASETUPSTREAMDATASETS (
	DATASETUPSTREAMDATASETSKEY NUMBER(38,0) NOT NULL AUTOINCREMENT START 1 INCREMENT 1,
	DATASETID CHAR(36) NOT NULL DEFAULT '',
	TARGETDATASETID CHAR(36) NOT NULL DEFAULT '',
	GROUPID CHAR(36) NOT NULL DEFAULT '',
        DATASOURCEKEY NUMBER(38,0) NOT NULL, 
        DELETEFLAG BOOLEAN NOT NULL DEFAULT FALSE, 
        EXTRACTEDON TIMESTAMP_NTZ(9), 
        CREATEDON DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP(),
	CREATEDBY VARCHAR(100) NOT NULL DEFAULT CURRENT_USER(),
	UPDATEDON DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP(),
	UPDATEDBY VARCHAR(100) NOT NULL DEFAULT CURRENT_USER(),
        primary key (DATASETUPSTREAMDATASETSKEY), 
        constraint FDATASOURCEKEY foreign key (DATASOURCEKEY) references PUBLIC.DIMDATASOURCES(DATASOURCEKEY) 
);

```
